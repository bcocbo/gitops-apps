apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-008app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: dev
    managed-by: backstage
spec:
  project: dev
  
  sources:
    - repoURL: https://github.com/bcocbo/eks_baseline_chart_Helm
      path: .
      targetRevision: HEAD
      helm:
        valueFiles:
          - $values/values.yaml
        releaseName: test-008app
    - repoURL: https://github.com/bcocbo/gitops-apps
      targetRevision: HEAD
      ref: values
      path: values/dev/test-008app
  
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
