apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-prueba2
  namespace: dev
  labels:
    app: test-prueba2
    app.kubernetes.io/name: test-prueba2
    app.kubernetes.io/instance: test-prueba2
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-prueba2
  template:
    metadata:
      labels:
        app: test-prueba2
    spec:
      containers:
      - name: test-prueba2
        image: nginxdemos/hello:latest
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: test-prueba2
  namespace: dev
  labels:
    app: test-prueba2
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: test-prueba2
